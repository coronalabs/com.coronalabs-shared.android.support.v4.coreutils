Sources for the plugin `shared.android.support.v4.coreutils`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "shared.android.support.v4.coreutils" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
